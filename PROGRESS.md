# 当前进度

更新时间：2026-08-10

## 本轮目标

- 加强第二章中央时间线的浓重水墨感，同时不遮挡两侧图片、文字和节点符号。
- 首页“龙泉青瓷”居中，使用更具书法感的字体。
- 标题随滚动化为轻量粒子，粒子渐隐并过渡为背景晕染，再衔接现有瓷器内部景象显影。
- 所有滚动动画保持自然、可逆，并兼顾移动端与离线包。

## 已完成

- 核对现有开场、第二章时间线、滚动同步逻辑和响应式安全区域。
- 确认现有横版与竖版首页素材已包含龙泉山水、窑场建筑、窑火和青瓷碎片，不再额外引入风格不一致或可能被误读为史料的外部图片。
- 确认时间线中央安全带宽度为：手机 `64px`、平板 `120px`、桌面 `160px`。
- 选定本地可打包的书法字体 `Ma Shan Zheng`，已加入项目依赖。
- 确定实现方式：使用离屏画布采样题字，再以数量受控的 SVG 粒子呈现；不使用可见 Canvas 或 WebGL。

## 尚未修改

- `index.html` 尚未加入题字粒子和墨晕承接层。
- `src/main.ts` 尚未接入题字采样、粒子序列和新的开场滚动编排。
- `src/styles.css` 尚未调整首页题字布局、书法字体、背景景深和水墨时间线样式。
- 视觉检测、类型检查、构建、离线打包和多尺寸截图检查尚未运行。

## 下一步

1. 将首页题字固定在视口正中，并为默认无脚本状态保留清晰可读的静态标题。
2. 生成受控 SVG 粒子，将“题字墨蚀 -> 粒子退散 -> 背景晕染 -> 瓷内景显影”接入现有 GSAP 可逆时间线。
3. 为第二章同一路径增加多层墨线、干笔断续与浓淡进度层，仍限制在中央安全带内。
4. 运行 Impeccable 检测一次，再进行手机与桌面的批量截图检查和一次集中修正。
5. 运行 `npm run typecheck`、`npm run qa:visual`、`npm run build` 和 `npm run package:offline`。

